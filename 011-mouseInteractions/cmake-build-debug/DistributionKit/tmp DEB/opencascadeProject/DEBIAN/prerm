#!/bin/bash

APPS=opencascadeProject

for app in "${APPS[@]}"
do

    SRC_FILE="$TARGET_DIR/$app.sh"
    rm "/usr/share/applications/$app.desktop"

    rm /usr/bin/"${app,,}"

    echo "Remove ${app,,}"
done
